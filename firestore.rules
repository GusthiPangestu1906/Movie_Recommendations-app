rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      match /favorites/{movieId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      match /favorite_actors/{actorId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      match /history/{movieId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
